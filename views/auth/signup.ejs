<div class="container">
  <div class="row">
    <h2 class="text-center">Regístrate en Bremen.io</h2>

    <form action="/auth/signup" method="POST" id="form-container" class="col-md-6 col-md-offset-3">
      <p class="reel-in">
        El registro es gratuito y te permitirá escribir a otros usuarios para unirte a sus proyectos, crear tus propios anuncios y más ventajas.
      </p>
      <div class="form-group">
      <label for="username">Alias</label>
      <input class="form-control" id="username" type="text" name="username">
    </div>
    <div class="form-group">
      <label for="password">Contraseña</label>
      <input class="form-control" id="password" type="password" name="password">
      </div>
      <div class="form-group">
        <label for="city">Ciudad</label><br>
        <select name="city" id="city">
          <% city.forEach((city) => { %>
            <option type="text" value="<%= city %>" class="form-control"><%= city %></option>
          <%  }) %>
        </select>
      </div>

      <div class="form-group">
        <label for="main-instr">Instrumento Principal</label><br>
        <select name="mainInstrument" id="instrument">
          <% mainInstrument.forEach((mainInstrument) => { %>
            <option type="text" value="<%= mainInstrument %>" class="form-control"><%= mainInstrument %></option>
          <%  }) %>
        </select>
      </div>

      <% if (typeof message !== "undefined") { %>
        <div class="error-message"><%= message %></div>
      <% } %>
      <br>

      <button type="submit" class="btn btn-success">Registrarme</button>
      </form>
    </div>
</div>
